import TermDataGrid from "../components/data-grids/term/TermDataGrid";

const TermsPage = () => {
  return (
    <div className="p-5">
      <TermDataGrid />
    </div>
  );
};

export default TermsPage;
